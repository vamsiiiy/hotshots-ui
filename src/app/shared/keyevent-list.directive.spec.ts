import { KeyeventListDirective } from './keyevent-list.directive';

describe('KeyeventListDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyeventListDirective();
    expect(directive).toBeTruthy();
  });
});
